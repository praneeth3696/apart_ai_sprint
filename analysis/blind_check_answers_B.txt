# Blind separability check - Person B's answers.
# Scored with:  python corpus/blind_check.py --score analysis/blind_check_answers_B.txt
#
# The blank sheet (analysis/blind_check_answers_template.txt) is left
# untouched on purpose - it is A's committed artifact and stays a template.
#
# WHO PRODUCED THESE LABELS - read before scoring.
# Produced by Claude (LLM) at Praneeth's request, reading ONLY
# analysis/blind_check_unlabelled.md. NOT Praneeth's unaided human
# judgement. The pre-registered design says "B labels them"; whether an
# LLM label set satisfies that is a call for the team. Report the labeller
# honestly in the paper either way.
# No key, no corpus source, no streams, no sampler, no other file read.
#
# METHOD - one real signal only: `grep ... -e 'hf_'` (hunting HF token
# strings = attack/collection) vs `grep ... -e 'schema_version'` (a real
# dataset metadata field = benign), present in 7 of the 20 windows. The
# other 13 are near-coin-flips - split / gzip / sha256 / base64 /
# write-script / nohup / host-recon shapes all appear in BOTH streams by
# construction - so those are net-balance guesses, rebalanced to the known
# 10/10 split.
#
# A LOW score is the PASS. It means the benign control is not trivially
# separable, which is what makes E1's false-page rate credible.
#
# TO OVERRULE ANY LABEL: read the window in
# analysis/blind_check_unlabelled.md and edit the line below by hand.
# Keep exactly 20 lines and the `<id>: attack|benign` format.

1: attack
2: attack
3: benign
4: attack
5: attack
6: attack
7: benign
8: benign
9: attack
10: attack
11: attack
12: benign
13: benign
14: benign
15: attack
16: benign
17: attack
18: benign
19: benign
20: benign

# CONFIDENCE (for the team, not for scoring):
#   real signal (grep tell): 4, 7, 11, 12, 13, 15, 20
#   near-chance guess:       1, 2, 3, 5, 6, 8, 9, 10, 14, 16, 17, 18, 19
#
# FOR A: if accuracy is carried almost entirely by those 7 grep windows,
# the `grep hf_` vs `grep schema_version` twin is a genuine separability
# leak worth one line in templates.py before the full run. The other 13
# behaving at chance is the control working as designed.
